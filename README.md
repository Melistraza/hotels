To setUp project just clone it and run docker-compose up

Test credentials to login

email: admin@mail.com
password: adminadmin

Docker container should be immutable so i use separate container for storing data
